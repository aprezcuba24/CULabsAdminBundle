{{ '{% extends \'/admin/%entity_name%/form.html.twig\' %}'|replace({'%entity_name%':entity|lower|replace({'\\': '/'})}) }}

{{ '{% block title_page %}' }}{{ '{{ \'New %entity_name%\'|trans }}'|replace({'%entity_name%':entityClass}) }}{{ '{% endblock %}' }}

{{ '{% block actions %}' }}
    <ul class="actions">
        <li>
            <a href="{{ "{{ path('"~ route_name_prefix ~"') }}" }}" class="btn btn-icon btn-info">
                <i class="icon-list icon-white"></i>{{ '{{ \'List\'|trans }}' }}
            </a>
        </li>
    </ul>
{{ '{% endblock %}' }}