{{ '{% form_theme form \'@CULabsAdmin/Form/fields.html.twig\' %}' }}
<form method="POST" {{ '{{ form_enctype(form)}}' }}>
    {{ "{{ form_widget(form) }}" }}
    <div class="form-actions">
        <input class="btn btn-primary" value="{{ '{{ \'Save\'|trans }}' }}" type="submit"/>
        <a href="{{ "{{ path('"~ route_name_prefix ~"') }}" }}" class="btn">{{ '{{ \'Cancel\'|trans }}' }}</a>
    </div>
</form>
